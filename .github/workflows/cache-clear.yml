name: cache-clear
run-name: ${{ github.workflow }} (${{ github.ref_name }})

on: workflow_dispatch

jobs:
  clear:
    runs-on: ${{ vars.RUNNER_IMAGE }}
    steps:
      - uses: snnaplab/delete-branch-cache-action@cacacb7f2e47069dad5f410c99c3e3aadecc09ca  # @v1